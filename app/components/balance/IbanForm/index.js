import React from 'react';

class IbanForm extends React.Component { // eslint-disable-line react/prefer-stateless-function
  
  static propTypes = {
  };

  static defaultProps = {
  };

  render() {
    return (
      <div>
      </div>
    );
  }
}

export default IbanForm;
